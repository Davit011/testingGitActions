name: compile

on:
  pull_request:
    branches:
      - '*'
  push:
    branches:
      - '*'

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - name: set up JDK 11
        uses: actions/checkout@v2
      - name: Maven clear
        run: mvn -B clean verify